import React from 'react'

import Drawer from './Main/drawers';

import Aux from '../../hoc/Aux';



const layout = (props) => (
    <Aux>
    <div><Drawer/></div>
    
    </Aux>
    );
    
    export default layout;